import React from "react";
import Nav from "../../../component/Nav/Nav";

//화면 Main(메인화면) 컴포넌트를 만든다
const Main = () => {

    return (
        <div>
            <p>여기는 치매를 진단할 수 있는 문항이 있는 페이지입니다.</p>
            <Nav />
        </div>
    );
};

export default Main;